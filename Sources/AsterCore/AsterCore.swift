/// AsterCore: resolution-independent geometry, models, rendering and export for AsterMark.
/// Must not depend on SwiftUI or AppKit — see docs/03-architecture.md.
public enum AsterCore {
    public static let projectSchemaVersion = 1
}
